# 环境与依赖检查记录（最终版，2026-03-27）

## 环境检查
- Java：21.0.10（通过）
- Maven：3.8.8（通过）
- Node：22.15.0（通过）
- npm：PowerShell 执行策略拦截，已改用 `cmd /c npm ...`（通过）
- Python：3.8.0（通过）
- MySQL：8.0.45，服务 `MySQL80` 运行（通过）

## 依赖安装与验证
- backend
  - `mvn -q -DskipTests compile`：通过。
  - 为避开全局 maven 仓库写权限问题，已启用项目本地仓库：`-Dmaven.repo.local=D:/IDEAWorkspace/fruit_warehouse_manage/backend/.m2repo`。
- frontend
  - `cmd /c npm run build`：通过。
- ai-algorithm
  - `pandas`/`numpy`：已就绪。
  - `pymysql`：安装成功。
  - `prophet`：安装失败（镜像 hash 不一致 + 官方源代理不可达），当前使用简化算法兜底。

## 数据库准备
- MySQL 密码采用你提供的 `root`。
- `application-dev.yml` 已同步为 `password: root`。
- 原始 SQL 文件编码损坏，无法直接导入；已改为：
  - `sql/_sanitized_schema.sql`（由 `01_schema.sql` 去注释清洗生成）
  - `sql/_seed_ascii.sql`（手工最小可运行种子数据）
- 数据库已成功重建并导入。