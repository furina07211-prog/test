# 项目清洗完成报告

## 1. 验收背景
- 项目：`fruit_warehouse_manage`
- 验收时间：2026-03-27
- 验收口径：仅界面文案必须中文；技术/数据原值（如枚举值、单位原值）允许保留，但面向用户展示应有中文映射。
- 本次执行范围：中文化完整性、核心功能可用性、一键启动、数据库导入、成品达标判定。

## 2. 环境与版本
- MySQL：8.0.45
- Maven：3.8.8
- JDK：21.0.10
- Node：11.12.0（npm）
- 前端：Vite 6.4.1（dev/build）

## 3. 执行记录与结果

### 3.1 全界面中文无遗漏校验
执行方式：
- 静态扫描 `frontend/src/views`、`frontend/src/layout`、`frontend/src/components` 文案残留关键字。
- 重点检索词：`Dashboard/Inventory/Purchase/Sales/No Data/Loading/Go to/Total/Smart/AI assistant/TOP5/SKU/kg/FW`。

结果：
- 通过（P0）。
- 未发现上述界面英文文案残留；分页、空态、加载文案均为中文。
- 发现 `kg`、`DRAFT/APPROVED` 等技术/数据值出现在代码或数据映射链路，属于允许保留项。

### 3.2 登录/库存/采购/销售/AI 功能校验

#### 登录
证据：
- `/api/auth/login` 三账号登录成功。
- `/api/auth/me` 三账号角色回读正确。

结果：通过。

#### 库存
证据：
- `/api/inventory/batches` 分页成功（code=200）。
- `/api/inventory/batches/low-safety`、`/near-expire` 返回正常。

结果：通过。

#### 采购全流程
证据：
- 新建采购单成功（code=200）。
- 提交/审核/收货依次成功，状态流转：`DRAFT -> SUBMITTED -> APPROVED -> RECEIVED`。
- 明细待收数量归零，且库存批次新增并数量一致（SQL核验通过）。

结果：通过。

#### 销售全流程
证据：
- 创建销售单接口返回失败：`code=500`。
- 关键错误：`Unknown column 'shipped_qty' in 'field list'`（`sales_order_item` 表缺字段）。

结果：不通过（阻塞）。

#### AI 功能
证据：
- `/api/ai/assistant/dispatch`、`/api/ai/assistant/history` 可调用（code=200）。
- `/api/ai/chat` 返回 `code=400`，提示 AI API Key 未配置。

结果：部分通过（非阻塞主流程，但不满足“AI功能全部正常”目标）。

### 3.3 一键启动无报错校验
执行：
- 后端：`mvn spring-boot:run` 启动成功，监听 `8080`。
- 前端：`npm run dev -- --host 0.0.0.0 --port 5173` 启动成功，监听 `5173`。
- 联通：
  - `http://localhost:5173` 可访问。
  - `http://localhost:8080/doc.html` 可访问。
  - `http://localhost:5173/api/auth/login` 代理调用成功。

结果：通过。

### 3.4 数据库导入无异常校验
执行脚本（按文档）：
1. `sql/_sanitized_schema.sql`
2. `sql/_seed_ascii.sql`

结果：
- 导入命令执行成功，无SQL语法错误。
- 但导入后的结构与当前销售代码不一致（缺 `sales_order_item.shipped_qty`），导致销售模块运行失败。

结论：导入过程通过，数据结构一致性不通过。

## 4. 问题清单

### 阻塞问题（P0）
1. `sales_order_item` 缺少 `shipped_qty` 字段
- 现象：销售创建接口 500。
- 影响：销售模块核心链路不可用。
- 归因：`sql/_sanitized_schema.sql` 与后端实体/Mapper不一致。

### 一般问题（P1）
1. AI 对话在未配置 API Key 时不可用
- 现象：`/api/ai/chat` 返回 `code=400`。
- 影响：AI模块无法满足“全部正常”演示目标。
- 备注：`dispatch/history` 可调用，说明AI编排链路部分可用。

## 5. 达标结论
- 综合判定：**当前版本未达“毕设成品达标”**。
- 不达标原因：存在 P0 阻塞（销售模块不可用），且 AI 对话能力未达“全部正常”目标。

## 6. 建议整改项（按优先级）
1. 先修复数据库结构与后端一致性：为 `sales_order_item` 补 `shipped_qty` 字段（并同步脚本）。
2. 提供 AI 无Key降级策略（规则回复/本地兜底），避免 `/api/ai/chat` 直接失败。
3. 复跑本报告同一套验收脚本，更新结论为“达标/不达标”。

## 7. 附录：关键命令摘要
- 数据库导入：
  - `mysql -h 127.0.0.1 -P 3306 -u root -p123456 fruit_warehouse_manage -e "source sql/_sanitized_schema.sql"`
  - `mysql -h 127.0.0.1 -P 3306 -u root -p123456 fruit_warehouse_manage -e "source sql/_seed_ascii.sql"`
- 后端启动：`mvn spring-boot:run`
- 前端启动：`npm run dev -- --host 0.0.0.0 --port 5173`
- 编译验证：
  - 后端 `mvn -q -DskipTests compile`
  - 前端 `npm run build`

---
> 备注：验收过程中已将本机 MySQL `root@localhost` 口令调整为 `123456`，以匹配项目固化配置并完成一键启动核验。
